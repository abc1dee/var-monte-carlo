"""
API Integration Tests

Tests:
- GET /api/health → 200
- GET /api/tickers → correct schema
- POST /api/simulate (valid input) → 200 + correct schema
- POST /api/simulate (invalid ticker) → 400
- POST /api/simulate (out-of-range params) → 422
"""

# TODO: Generate with Claude Web — Prompt 6 (Tests)
# See developer_workflow_guide.md Section 3.3, Prompt 6
